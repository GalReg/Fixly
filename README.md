# Fixly 🤖🔧

**Fixly: Ремонт по фото с помощью AI**

AI-ассистент, который по описанию бытовой проблемы определяет вероятную причину поломки и помогает подготовиться к ремонту.

## 🎯 Проблема

Каждый третий выезд мастера по ремонту оказывается "пустым" из-за неточной информации от клиента. Это приводит к:
-   **Потере времени**: до 3 часов на один выезд (дорога + диагностика).
-   **Потере денег**: до 50 000 ₽ в месяц упущенной выгоды и расходов на топливо.

Мастера едут "вслепую", не зная, какие инструменты и запчасти понадобятся.

## ✨ Решение

**Fixly** — это AI-ассистент, который берет на себя первичную диагностику:

1.  **Сбор информации**: Пользователь (клиент) отправляет описание проблемы через Telegram-бот.
2.  **Диалог и анализ**: AI-ассистент задает уточняющие вопросы для сбора всех необходимых деталей.
3.  **Формирование отчета**: Система генерирует отчет для мастера, который включает:
    -   Вероятный "диагноз" поломки.
    -   Список необходимых запчастей.

### Кому это нужно?

-   **Мастерам**: чтобы сократить время на общение с клиентом, исключить "пустые" выезды и приезжать на заказ уже подготовленными.
-   **Клиентам**: чтобы заранее понимать примерный объем работ и затраты, а также убедиться в компетентности мастера.

## Отчёты по чекпоинтам
Чекпоинт 1: https://github.com/GalReg/Fixly/blob/main/docs/Report_checkpoint1.md

Чекпоинт 2: https://github.com/GalReg/Fixly/blob/main/docs/Report_checkpoint2.md

Чекпоинт 3: https://github.com/GalReg/Fixly/blob/main/docs/Report_checkpoint3.md

Демонстрация работы: https://github.com/GalReg/Fixly/blob/main/fixly.gif

## 🚀 Технологический стек (план)

-   **Backend**: Python
-   **AI/ML**: LLM (Large Language Models), RAG (Retrieval-Augmented Generation)
-   **Интерфейс**: Telegram Bot API
-   **База данных**: Векторная база данных (FAISS) для RAG

## ⚙️ Настройка проекта

Для обеспечения качества кода в проекте используются `ruff` и `pre-commit`.

1.  Установите зависимости для разработки:
    ```bash
    pip install ruff pre-commit
    ```
2.  Активируйте git-хуки:
    ```bash
    pre-commit install
    ```
При каждом коммите код будет автоматически проверяться и форматироваться.

## Структура проекта

```
.
├── ifixit_data/          # Директория для сырых .txt файлов, извлеченных из ZIM
├── vectorstore/
│   ├── db_faiss/         # Финальная векторная база данных FAISS
│   └── tmp/              # Временные файлы при создании базы
│
├── extract_ifixit.py     # 1. Скрипт для извлечения и очистки данных из ZIM-архива
├── create_vector_db.py   # 2. Скрипт для чанкинга и векторизации данных (создание чекпоинтов)
├── merge_vector_db.py    # 3. Скрипт для объединения чекпоинтов в единую базу
├── bot_w_langchain_rag.py# 4. Основной файл с логикой Telegram-бота и RAG
│
├── requirements.txt      # Список зависимостей Python
├── Dockerfile           # Конфигурация Docker контейнера
├── docker-compose.yml   # Конфигурация Docker Compose
├── .dockerignore        # Исключения для Docker
├── .env.example         # Шаблон файла с переменными окружения
└── README.md
```

## 🐳 Docker Configuration

Проект поддерживает запуск в Docker контейнере для упрощения развертывания.

### Предварительные требования

- Установленный Docker и Docker Compose
- Созданный Telegram бот и его токен
- API ключ Yandex GPT и Folder ID

### Настройка

1. Скопируйте файл с переменными окружения:
   ```bash
   cp .env.example .env
   ```

2. Отредактируйте файл `.env` и добавьте ваши реальные значения:
   ```bash
   TELEGRAM_BOT_TOKEN=your_actual_bot_token
   YANDEX_API_KEY=your_actual_yandex_api_key
   YANDEX_FOLDER_ID=your_actual_folder_id
   ```

### Запуск с Docker Compose

```bash
# Сборка
docker-compose build telegram-bot

# Запуск контейнера
docker-compose up -d

# Просмотр логов
docker-compose logs -f telegram-bot

# Остановка контейнера
docker-compose down
```
